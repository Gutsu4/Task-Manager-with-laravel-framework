import React from 'react';
import {LoginForm} from '../parts/form';

const Login = () => {
    document.title = 'Login';
    return (
        <div>
            <LoginForm />
        </div>
    );
};

export default Login;
